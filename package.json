{
  "name": "npm-test",
  "version": "1.0.0",
  "description": "一个简单的 npm 包",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "creat":"mkdir src && cd src",
    "dev": "parcel serve src/index.html",
    "build":"parcel build src/index.html --experimental-scope-hoisting"
  },
  "author": "zhangshuai",
  "license": "ISC",
  "dependencies": {
    "parcel-bundler": "^1.12.5",
    "date-fns":"^2.30.0"
  }
}
